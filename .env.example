# Copy to `.env` in this folder (gitignored). Restart Metro after changes.
# Remote processing-pipeline API (Swagger: /docs on same host:port).
EXPO_PUBLIC_AI_SCRIBE_API_URL=http://98.88.67.46:8100
# API key for the backend auth middleware (ask your backend developer for this value).
EXPO_PUBLIC_AI_SCRIBE_API_KEY=
